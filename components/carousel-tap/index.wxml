<view>
  <scroll-view scroll-x="true" class="image_group" bindscroll="scrollView" scroll-into-view="{{toView}}" scroll-with-animation="true">
      <view wx:for="{{carousels}}" id="{{'item'+index}}" class="image_wrapper">
        <image class="scroll_image" src="{{item.pic}}" />
        <text>{{item.name}}</text>
      </view>
    </scroll-view>

<button bindtap="change">切换</button>
</view> 

 